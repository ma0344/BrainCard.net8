<UserControl x:Class="MyUWPApp.CustomCard"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="using:MyUWPApp"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DesignWidth="{Binding cardWidth}"
             d:DesignHeight="{Binding cardHeight}"
             Width="{Binding cardWidth}"
             Height="{Binding cardHeight}"
             IsHitTestVisible="True"
             Background="White">
    <Grid x:Name="baseGrid"
          Background="White"
          IsHitTestVisible="True">
        
        
        <Thumb x:Name="CardThumb"
               Margin="1"
               BorderThickness="0"
               BorderBrush="black"
               Background="White"
               Width="{Binding cardWidth}"
               Height="{Binding cardHeight}"
               IsHitTestVisible="True"
               >
            <Thumb.Template>
                <ControlTemplate TargetType="Thumb" >

                        
                    <InkCanvas x:Name="CardCanvas"
                               Width="{Binding cardWidth}"
                               Height="{Binding cardHeight}"
                               Loaded="InkCanvas_OnLoaded"
                               HorizontalAlignment="Stretch"
                               VerticalAlignment="Stretch"
                               IsHitTestVisible="True">
                        
                        <InkCanvas.RenderTransform>
                            <ScaleTransform x:Name="CardCanvasRenderTransform" />
                        </InkCanvas.RenderTransform>
                    </InkCanvas>
                </ControlTemplate>
            </Thumb.Template>

        </Thumb>
    </Grid>
</UserControl>
